import React from 'react'

function SearchBar() {
  return (
    <div>
      <input type="search" placeholder="Seek A Character"></input>
      <button className='btn btn-primary'>?</button>
    </div>
  );
}

export default SearchBar